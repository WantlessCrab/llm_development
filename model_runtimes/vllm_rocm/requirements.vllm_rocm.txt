# Minimal runtime support packages.
# vLLM itself is installed separately in the Dockerfile with the selected ROCm wheel index.

uv>=0.5.0
openai>=1.40.0
httpx>=0.27.0
requests>=2.31.0
huggingface-hub[cli]>=0.25.0
safetensors>=0.4.0